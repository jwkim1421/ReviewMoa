# 리뷰모아 다음 단계 업그레이드 계획

이 문서는 2026-08-15 현재 리뷰모아의 구현 상태를 기준으로 앞으로 검토할 기능과
현실적으로 진행할 업데이트를 정리한다. `HANDOFF.md`의 과거 계획보다 이 문서의
우선순위가 최신이며, 각 항목은 한 번에 하나씩 구현하고 검증한 뒤 다음 항목으로
넘어간다.

단, 현재 진행 중인 항목이 **운영자가 통제할 수 없는 외부 요인**(예: Apple 멤버십
활성화, 쇼핑몰 접근성 회복, 외부 심사)으로 대기 상태라면, 그 항목을 닫지 않은 채
다음 순위의 **코드로 바로 진행 가능한 항목**을 병행한다. 이때도 병행 항목은 한 번에
하나만 진행하며, 각 항목의 완료 기준은 독립적으로 통과시킨다.

## 진행 현황

| 항목 | 상태 | 마지막 업데이트 |
|---|---|---|
| P0-1 네이버 전용 어댑터 안정성 | 🟡 코드 대폭 보강(0건 탭 감지·최신순 전체스캔 폴백·오매칭 방지), 실기기 검증 진행 중 | 2026-08-15 · 확장 0.1.25 (134) |
| P0-2 구조화된 오류 진단과 운영자 화면 | 🟢 코드 완료·Worker 배포됨 (ADMIN_TOKEN secret 등록 여부 확인) | 2026-08-15 |
| P0-3 자동 회귀 테스트와 네이버 canary | 🟢 완료(첫 정기 실행 확인만 남음) | 2026-08-10 |
| P0-4 TestFlight 가족 베타 | 🟢 완료 — 멤버십·앱등록·업로드·App Privacy·외부심사 승인·가족 설치·테스트 중 | 2026-08-13 · 0.1.25 (134) |
| P0-5 릴리스 체크리스트·버전 자동화 | 🟡 부분 — 빌드번호 자동증가만 존재, `docs/release-checklist.md` 미작성 | 2026-08-15 |
| P0-6a 보안·인증(SSRF·rate limit·collector 토큰) | 🟢 완료·배포됨 (코드리뷰 수정 포함) | 2026-08-15 |
| P0-6b 보존 정책(원문 분리·cron 삭제) | 🟢 완료·배포됨 | 2026-08-15 |
| HANDOFF 이월: 작업 상태 전이 검증 | 🟢 기존 상태 가드 + 회귀 테스트로 고정(P0-6a) | 2026-08-11 |
| HANDOFF 이월: `products/resolve`·refresh 정리 | ⚪ 대기 | P1-5와 함께 검토 |
| P1-1 분석 정확도·신뢰도 개선 | 🔵 다음 진행 항목 | 2026-08-15 |
| P1-2~P2-4 | ⚪ 대기 | 항목별 순차 진행 |

계획 외 이번 세션 추가 작업: 앱/확장 아이콘·파비콘, 웹 UI 개선(0건 빈 상태 보고서,
네이버 플레이스 안내, 팝업 "현재 페이지 리뷰 수집", 어절 줄바꿈·레이아웃·URL 지우기
버튼), 빌드 번호 동기화, 코드리뷰 지적 1~7 반영. P2 백로그에 네이버 플레이스(장소)
리뷰 지원을 추가했다.

P0-1 0.1.24에서는 모바일 브랜드스토어·스마트스토어·쇼핑 카탈로그 URL 유형을
구분하고, 어댑터 진단에 페이지 유형·상품 ID·수집 전략·별점별 목표를 기록한다.
네이버 전체 리뷰 제목의 리뷰 수를 읽을 수 있으면 원본 별점 분포 합계와 교차검증해
불일치 보고서 생성을 차단한다. 자동 테스트와 iOS 시뮬레이터 빌드는 통과했으며 실제
iPhone에서 추가 상품 검증이 남아 있다.

0.1.25에서는 수집 오버레이와 카드에 `border-box` 너비 계산을 적용해 iPhone
Safari에서 좌우 여백이 동일하게 유지되도록 수정했다.

## 1. 현재 상태

현재 가능한 기능은 다음과 같다.

- iPhone Safari 확장에서 네이버 상품 페이지 열기
- CAPTCHA 또는 로그인 화면이 나타난 경우 사용자가 직접 해결한 뒤 자동 재개
- 전체 리뷰 목록 열기와 최신순 확인
- 네이버 원본 별점 분포 확인
- 별점별 최대 100개 리뷰 수집
- 광고성, 중복, 평점 불일치, 판단 유보 분류
- 별점별 대표 리뷰 원문 표시
- 좋은 점, 아쉬운 점, 구매 결론 분석
- 수집 완성도와 신뢰도 점수 표시
- 작업 중단, 재개, 캐시, 다시 불러오기
- 수집 중 오버레이와 원래 리뷰모아 탭 복귀
- 중앙 Mac collector와 iPhone Safari 수집 경로 공존
- GitHub Pages 웹과 Cloudflare Worker/D1 운영

현재 핵심 한계는 다음과 같다.

- 네이버 외 쇼핑몰은 아직 실사용 수준으로 검증되지 않았다.
- 네이버 DOM이 바뀌면 어댑터를 지속적으로 보수해야 한다.
- 오류 진단 정보가 여러 위치에 흩어져 운영자가 한 화면에서 확인하기 어렵다.
- 무료 Personal Team 앱은 7일마다 다시 설치해야 한다.
- 규칙 기반 분석이 다양한 상품군의 표현을 모두 이해하지 못한다.
- 옵션이 다른 리뷰가 하나의 분석에 섞일 수 있다.
- 데이터 만료 정리가 일반 API 요청에 의존한다.
- 쇼핑몰 접근성 검증 문서가 최근 성공 결과를 충분히 반영하지 못했다.

## 2. 기능 브레인스토밍

### 2.1 수집과 쇼핑몰 지원

- 네이버 브랜드스토어, 스마트스토어, 쇼핑 카탈로그 전용 어댑터
- 네이버 모바일과 PC DOM 별도 처리
- 쿠팡, 컬리, 오늘의집, 11번가, SSG, G마켓 전용 어댑터
- 특정 브랜드 자사몰 전용 어댑터
- P2 백로그: 네이버 플레이스(장소) 리뷰 지원 — 공식 API 없음, 전용 어댑터 + 무별점 분석 필요
- 전체 리뷰 수와 별점 분포 자동 검증
- 최신순 정렬 자동 검증
- 상품 옵션별 리뷰 필터링
- 판매처 사이의 동일 리뷰 중복 제거
- 마지막 수집 이후 새 리뷰만 추가하는 증분 수집
- 리뷰 이미지, 동영상, 도움돼요 수, 구매 인증 여부 수집
- 리뷰가 적을 때 동일 상품의 대체 판매처 제안
- 사이트별 지원 상태 표시
- 문제가 생긴 어댑터를 원격으로 중지하는 비상 스위치

### 2.2 분석 기능

- 상품 옵션, 사용 목적, 연령대별 분석
- 장기 사용 후기만 따로 분석
- 배송과 판매자 평가를 제품 평가에서 분리
- 최근 리뷰와 과거 리뷰의 의견 변화
- 별점과 본문 감정 불일치 탐지 개선
- 유사 문장과 복사 리뷰 탐지 강화
- 체험단, 협찬, 과도한 반복 문구 탐지 강화
- 상품군별 전용 분석 규칙
- AI 기반 주제 자동 군집화
- 분석 결론과 근거 원문 연결
- 장단점별 언급 횟수와 비율 표시
- 별점 분포 시각화와 신뢰도 상세 설명
- 구매 전 질문 자동 생성
- 한 줄 요약과 상세 요약 선택
- 두 상품 또는 여러 판매처 비교
- 가격 대비 만족도와 리뷰 급증 패턴 분석

### 2.3 사용자 기능

- 최근 확인한 상품, 즐겨찾기, 저장한 보고서
- 보고서 공유 링크와 가족 공유
- 상품 재검사와 이전 결과 비교
- 새 리뷰, 평점 또는 가격 변화 알림
- 여러 상품 비교함
- PDF 저장
- 접근성, 큰 글씨, 다크 모드
- 오류 제보와 분석 피드백
- 지원 쇼핑몰 목록과 수집 예상 시간 표시
- 현재 수집 중인 별점과 리뷰 수 표시
- CAPTCHA 대기 상태 개선

### 2.4 모바일 앱

- Safari 공유 메뉴에서 리뷰모아 실행
- URL 자동 전달과 앱 내 진행 상태
- 수집 완료 푸시 알림
- 최근 보고서, 즐겨찾기, 다시 검사, 비교
- Safari 확장 설정과 권한 상태 자동 진단
- iPad 지원
- 필요성이 확인된 뒤 Android 앱 또는 Firefox 확장 검토

### 2.5 운영과 관리

- 운영자 대시보드
- 작업, 사이트, 확장 버전별 성공률
- 오류 코드별 통계와 정제된 DOM 진단
- 어댑터별 실상품 정기 검증
- 배포 전 자동 회귀 테스트
- 수집 성공률 하락 알림
- D1 백업과 정기 데이터 만료
- 사용자별 요청 제한과 악성 URL 차단
- 어댑터 비상 중지와 기능 플래그
- 개발, 베타, 운영 환경 분리
- 개인정보 삭제 요청, 이용약관, 개인정보 처리방침
- App Store 권한 설명과 비용 사용량 제한

### 2.6 장기 실험 아이디어

- 여러 쇼핑몰의 동일 상품 평판 비교
- 모델 번호 또는 GTIN 기반 상품 자동 매칭
- 화면 캡처 OCR 리뷰 분석
- 제품 사진에서 모델명 추출
- 사용자 조건에 따른 맞춤 구매 결론
- 가격 추적과 리뷰 품질을 결합한 구매 시점 추천
- 공식 API만으로 수집 가능한 출처 탐색
- 오류를 AI가 분석해 어댑터 수정안과 회귀 테스트를 제안하는 기능

AI 자동 오류 수정과 자동 배포는 보류한다. 재검토하더라도 `오류 수집 → 개인정보
제거 → AI 수정안 → fixture 테스트 → 실제 canary → 운영자 승인 → 배포` 순서를
지키며, AI가 검증 없이 운영 코드를 직접 변경하게 하지 않는다.

## 3. 구현 난이도 기준

- **S**: 0.5~2일
- **M**: 3~7일
- **L**: 1~3주
- **XL**: 1개월 이상

개발자 1명 기준의 대략적인 규모이며 외부 심사, 사이트 변경, 접근 제한에 따른 대기
시간은 포함하지 않는다.

## 4. 현실적으로 진행할 업데이트

## P0-1. 네이버 전용 어댑터 안정성 고정

**목표**

네이버 페이지가 일부 변경되거나 URL 유형이 달라도 잘못된 보고서를 만들지 않고,
원본 별점 분포와 실제 수집 결과를 검증한 뒤에만 분석을 완료한다.

**업데이트 위치**

- `extension/content.js`
- `extension/site-configs.js`
- `extension/content.test.ts`
- `collector/src/adapters/naver.ts`
- `collector/tests/naver-adapter.test.ts`
- `docs/access-matrix.md`

**구현 방법**

- 브랜드스토어, 스마트스토어, 모바일 페이지, 쇼핑 카탈로그 유형을 구분한다.
- 전체 리뷰 목록, 최신순, 별점 분포, 별점 필터, 페이지 이동을 독립 단계로 만든다.
- 클릭 성공이 아니라 URL과 실제 리뷰 DOM의 변화를 검증한다.
- 원본 별점 분포와 별점별 수집 수를 대조한다.
- 리뷰 날짜가 최신순인지 확인한다.
- 리뷰 수와 페이지 유형이 다른 실제 상품 10~15개를 고정 검증한다.
- 안전하게 정제한 HTML fixture와 회귀 테스트를 추가한다.
- 검증 실패 시 `collection_incomplete`로 중단하고 보고서를 만들지 않는다.

**난이도**: M~L

**장점**

- 현재 제품의 가장 중요한 신뢰성 문제를 줄인다.
- 네이버 DOM이 바뀌어도 실패 단계를 빨리 찾을 수 있다.
- 대표 리뷰 일부만 전체 리뷰로 오인하는 회귀를 예방한다.

**단점**

- 네이버 화면 변경에 따른 지속적인 유지보수가 필요하다.
- 모바일과 PC DOM을 함께 관리해야 한다.

**완료 기준**

- 검증 상품의 원본 별점 분포와 수집 결과가 일치한다.
- 각 별점에서 최대 100개까지 최신 리뷰를 수집한다.
- 동일 상품을 반복 수집해도 일관된 결과를 얻는다.
- 실패 시 부정확한 보고서가 생성되지 않는다.

## P0-2. 구조화된 오류 진단과 운영자 화면

**구현 결과 (2026-08-09)**

- 기존 `jobs.progress_json`을 활용해 별도 마이그레이션 없이 최근 작업 진단을 조회한다.
- `ADMIN_TOKEN` Bearer 인증이 적용된 `GET /v1/admin/diagnostics`를 추가했다.
- `/#admin` 숨김 운영 화면에서 최근 작업, 종료 작업 성공률, 사이트·확장 버전별 결과,
  오류 유형, 재시도 가능 여부와 정제된 어댑터 진단을 확인할 수 있다.
- 상품 URL·상품명·리뷰 원문·쿠키·토큰·DOM 제어 원문은 관리자 응답에서 제거한다.
- 공개 작업 조회 응답에서도 내부 `collectorDiagnostics`를 제거한다.
- 운영 반영에는 Worker의 `ADMIN_TOKEN` secret 등록과 Worker·웹 배포가 남아 있다.

**업데이트 위치**

- `extension/content.js`, `extension/background.js`
- `worker/src/index.ts`와 신규 D1 마이그레이션
- `src/App.tsx` 또는 별도 운영자 화면
- `collector/src/*`

**구현 방법**

- 작업 ID, 사이트, 상품 ID, 확장과 어댑터 버전, 수집 단계, 원본 분포, 별점별
  수집 수, 시도한 제어 요소, 최신순 검증, CAPTCHA·로그인 감지, 오류 코드를 저장한다.
- 쿠키, 토큰, 리뷰 원문과 개인정보는 진단 정보에서 제거한다.
- 운영자 화면에 최근 실패, 사이트와 버전별 성공률, 오류 유형, 재시도 가능 여부를
  표시한다.

**난이도**: M

**장점**: 스크린샷 없이 실패 원인을 빠르게 확인하고 버전별 문제를 찾을 수 있다.

**단점**: 운영자 인증, 진단 데이터 보존 기간, 개인정보 정제가 필요하다.

## P0-3. 자동 회귀 테스트와 네이버 canary

**구현 결과 (2026-08-09)**

- 실제 모바일 전체 리뷰 DOM을 축약한 공유 fixture를 추가했다.
- Safari 확장과 중앙 collector 양쪽에서 전체 목록, 별점 1~5 분포, 별점별 추출,
  작성일 기준 최신순과 UI 문구 본문 정제를 검사한다.
- live canary 품질 정책에 전체 목록, 최소 리뷰 수, 필수 별점, 작성일 비율, 최신순,
  중복 ID, 별점별 100개 제한과 본문 정제를 포함했다.
- CAPTCHA·접근 제한·로그인 요구는 자동 재시도 없이 `blocked`와 종료 코드 2로 끝낸다.
- 실제 네이버 상품 한 개를 주 1회만 여는 별도 GitHub Actions workflow를 추가했다.
- 모든 push와 pull request 및 웹·Worker 배포 전에 `npm run release:gate`를 실행해
  fixture 또는 전체 테스트 기준이 깨지면 배포 단계를 차단한다.
- 실제 Chrome canary는 iPhone Safari 실기기 검증을 대체하지 않는다. 첫 정기 실행
  결과 확인은 운영 반영 후 남아 있다.
- 격리된 로컬 Chrome으로 첫 수동 canary를 한 번 실행했고 네이버 시스템 오류가
  `operator_required` / `blocked` / 종료 코드 2로 재시도 없이 중단되는 것을 확인했다.

**GitHub 인증과 Actions 권한 업데이트 (2026-08-10)**

- 커밋 `2bccc8d`를 `origin/master`에 처음 푸시할 때 GitHub가
  `.github/workflows/naver-canary.yml` 생성 요청을 거부했다.
- 원인은 코드나 저장소 쓰기 권한이 아니라, HTTPS Git 인증에 사용한 GitHub CLI OAuth
  토큰에 `repo` 범위만 있고 `workflow` 범위가 없었던 것이다.
- GitHub OAuth의 `workflow` 범위는 사용자가 원래 접근 가능한 저장소에서
  `.github/workflows/` 아래의 Actions workflow 파일을 추가하거나 수정할 수 있게 한다.
  저장소에 대한 기존 사용자 권한을 넘어서는 새로운 관리자 권한을 주지는 않는다.
- 다음 명령으로 GitHub CLI 토큰의 범위를 갱신하고 브라우저 기기 인증을 승인했다.

  ```bash
  gh auth refresh -h github.com -s workflow
  ```

- 갱신 후 `gh auth status`에서 `gist`, `read:org`, `repo`, `workflow` 범위를 확인했고,
  동일 커밋을 `origin/master`에 정상 푸시했다. 토큰 원문은 macOS 키체인에 보관되며
  저장소와 이 문서에는 기록하지 않는다.
- 이 OAuth `workflow` 범위는 개발자 PC에서 workflow YAML을 **푸시하는 권한**이다.
  Actions 실행 중 GitHub가 발급하는 `GITHUB_TOKEN` 권한과는 별개다.
- workflow 실행 권한은 각 YAML의 `permissions:`에서 최소 범위로 제한한다.
  품질 게이트와 네이버 canary는 `contents: read`, Pages 배포는 `contents: read`,
  `pages: write`, `id-token: write`만 사용한다. 저장소의 전역 Actions 권한 설정이나
  GitHub Secret 값은 이번 갱신에서 변경하지 않았다.
- 향후 같은 거부 메시지가 나오면 `gh auth status`로 `workflow` 범위를 확인한다.
  권한을 철회하려면 GitHub 계정의 `Settings → Applications → Authorized OAuth Apps`에서
  GitHub CLI 승인을 재검토하거나 철회한 뒤 필요한 최소 범위로 다시 인증한다.

**업데이트 위치**

- `extension/content.test.ts`
- `collector/tests/`, `test/fixtures/`
- `collector/src/canary.ts`
- `.github/workflows/`
- `docs/access-matrix.md`

**구현 방법**

- 모든 커밋에서 전체 리뷰 버튼, 별점 분포, 최신순, 별점별 추출, 본문 정제를 fixture로
  검사한다.
- 낮은 빈도의 실제 상품 canary를 별도로 실행한다.
- CAPTCHA나 접근 제한은 우회하지 않고 즉시 중단한다.
- 수집 결과가 기준에 미달하면 배포하지 않는다.

**난이도**: M

**장점**: 기존 동작을 다시 깨뜨리는 회귀를 예방하고 사이트 변경을 빠르게 발견한다.

**단점**: 실제 canary는 일시적으로 실패할 수 있고 너무 자주 실행하면 안 된다.

## P0-4. TestFlight를 통한 가족 베타 배포

**구현 결과와 현재 중단 지점 (2026-08-10)**

- Apple Developer Program 가입 절차를 완료했으며 Apple의 멤버십 활성화를 기다리고
  있다.
- Xcode Apple 계정 재로그인과 프로파일 다운로드를 완료했다.
- 앱과 Safari Extension 두 타깃 모두 `Personal Team`이 아닌 `Jinwook Kim` Team
  (`87KKJ3H226`)으로 설정했다.
- 앱 `kr.reviewmoa.ReviewMoa`, 확장 `kr.reviewmoa.ReviewMoa.Extension` Bundle ID와
  TestFlight 빌드 번호를 `safari/release.json`에서 관리한다.
- 릴리스 준비 시 빌드 번호 증가, WebExtension 리소스, 첫 실행 안내, Bundle ID,
  마케팅 버전과 1024px App Store 아이콘을 생성된 Xcode 프로젝트에 동기화한다.
- 앱 첫 실행 화면에 확장 활성화, 사이트 접근 권한, 리뷰모아 열기와 개인정보
  처리방침 안내를 추가했다.
- 개인정보 처리방침 페이지와 가족 외부 테스터 초대 절차를 문서화했다.
- 확장 0.1.25, 빌드 126으로 시뮬레이터 Release 빌드와 실기기용 Archive를 완료했다.
- 전체 테스트 125개와 웹, Worker, 중앙 collector 빌드를 통과했다.
- Xcode 재로그인 후 App Store Connect용 export를 다시 검증했다. 프로젝트의 Team
  선택은 정상이나 Apple 서버가 `No provider associated with App Store Connect user`
  및 `does not have permission to create iOS App Store provisioning profiles`를 반환했다.
  코드나 Bundle ID 문제가 아니라 Apple Developer Program 멤버십과 App Store Connect
  provider가 아직 활성화되지 않은 상태다.
- App Store Connect 앱 생성, 빌드 업로드, 피드백 이메일 입력과 첫 외부 TestFlight
  심사 및 가족 초대는 사용자 계정에서 진행할 단계로 남아 있다.

**멤버십 활성화 후 재개 순서**

1. Apple Developer 계정에서 Membership 상태가 `Active`인지 확인한다.
2. 동일한 Apple 계정으로 App Store Connect의 `나의 앱` 화면에 접근되는지 확인하고
   새 약관이 있으면 Account Holder로 동의한다.
3. `ReviewMoa` iOS 앱 레코드를 Bundle ID `kr.reviewmoa.ReviewMoa`로 생성한다.
4. `npm run safari:export`를 다시 실행해 앱과 확장의 App Store 배포 프로파일 및 IPA
   생성을 검증한다.
5. Archive를 App Store Connect에 업로드하고 빌드 처리를 기다린다.
6. 실제 피드백 이메일, 베타 설명과 검토 연락처를 입력해 첫 외부 TestFlight 심사를
   요청한다.
7. 승인 후 아내의 Apple 계정 이메일을 외부 테스터로 초대하고, 첫 실행 안내에 따라
   Safari 확장 활성화와 사이트 접근 권한을 설정한다.

**업데이트 위치**

- `safari/README.md`
- Xcode 프로젝트와 `extension/manifest.json`
- `scripts/prepare-safari-extension.mjs`
- App Store Connect와 개인정보 처리방침

**구현 방법**

- Apple Developer Program 가입과 고정 Bundle ID를 사용한다.
- 앱과 Safari Extension 타깃을 같은 개발팀으로 서명한다.
- 버전과 빌드 번호를 자동 증가시킨다.
- Archive를 App Store Connect에 올리고 TestFlight 테스터를 초대한다.
- 첫 실행에 확장 활성화와 웹사이트 접근 권한 안내를 넣는다.

**난이도**: M

**장점**: 7일마다 케이블로 다시 설치하지 않고 TestFlight로 업데이트할 수 있다.

**단점**: Apple Developer Program 비용, 베타 심사, 90일 빌드 만료가 있다.

## P0-5. 릴리스 체크리스트와 버전 자동화

**업데이트 위치**

- `package.json`, `extension/manifest.json`
- `scripts/prepare-safari-extension.mjs`
- `.github/workflows/pages.yml`, `.github/workflows/worker.yml`
- 신규 `docs/release-checklist.md`

**구현 방법**

- 웹, Worker, 확장 버전을 함께 기록한다.
- 테스트, 빌드, Safari package, iOS build, 마이그레이션, API 호환성, canary를 배포
  체크리스트로 묶는다.
- 확장 버전을 사용자와 진단 화면에서 확인할 수 있게 한다.

**난이도**: S~M

**장점**: 최신 버전 혼란과 배포 누락을 줄인다.

**단점**: 초기 릴리스 스크립트 정리가 필요하다.

**완료 기준**

- 웹·Worker·확장·어댑터 버전이 한 곳(예: `docs/release-checklist.md`와 진단 화면)에서
  일치하게 확인된다.
- 배포 체크리스트(테스트, 빌드, Safari package, iOS build, 마이그레이션, API
  호환성, canary)를 문서대로 따르면 배포 누락 단계가 없다.
- 사용자와 운영자 진단 화면에서 현재 확장 버전을 확인할 수 있다.

## P0-6a. 공개 API 보안과 요청 제한

`HANDOFF.md`의 P0 보안 항목(인증 없는 `/reviews`·`/complete` 호출 가능, SSRF,
CORS는 인증이 아님)을 이 항목으로 흡수한다.

**구현 결과 (2026-08-11)**

- 공개 작업 생성에 소스 화이트리스트(`ALLOWED_SOURCES` 7개)와 상품 ID 검증을 추가해
  미허용 소스는 `UNSUPPORTED_SOURCE`(400)로 거부한다. 임의 호스트로 향하는 작업 생성과
  미지원 사이트의 무한 대기를 막는다.
- `rate_events` 테이블(마이그레이션 `0005`)과 원자적 예약으로 IP 시간당 30건·전체 일일
  300건 제한을 적용하고 초과 시 `RATE_LIMITED`(429)를 반환한다. 캐시·중복 병합 응답은
  제한에서 제외해 정상 사용을 막지 않는다.
- 기존에 이미 갖춰진 collector·mobile 토큰 인증과 공개 `/reviews`·`/complete` 차단,
  잘못된 상태에서의 전이 거절(WHERE 가드), 완료 작업의 중복 complete 시 AI 재호출 없이
  저장 보고서 반환을 회귀 테스트로 고정했다.
- Worker 배포는 수동(`worker.yml`)이므로 코드 반영에는 `wrangler deploy`가 남아 있다.

**업데이트 위치**

- `worker/src/index.ts`
- `worker/wrangler.toml`
- 필요 시 신규 D1 마이그레이션

**구현 방법**

- 익명 기기 또는 세션 토큰과 완만한 요청 제한(사용자별·전체 일일)을 적용한다.
- 동일 URL의 중복 작업은 캐시 또는 활성 작업으로 합친다.
- collector·mobile 전용 endpoint는 `COLLECTOR_TOKEN` 또는 작업별 일회성 토큰을
  요구하고, 사용자 API로는 리뷰 업로드·complete를 호출할 수 없게 한다.
- 허용된 `https` 쇼핑몰 호스트만 열고 localhost·사설 IP·`file:`·`data:`·리디렉션 후
  사설망 URL을 서버와 collector 양쪽에서 차단한다(SSRF 방어).
- (HANDOFF 이월) 허용된 작업 상태 전이 표를 정의하고, 잘못된 상태에서의
  reviews·complete·resume 호출과 완료 작업의 중복 complete·AI 중복 호출을 거절한다.

**난이도**: M

**장점**: 인증 우회·SSRF·중복 AI 호출로 인한 남용과 비용을 막는다.

**단점**: 정상 사용자를 막지 않도록 제한을 조정해야 한다.

**완료 기준**

- 토큰 없는 collector·mobile 요청은 401이며 작업 존재 여부를 노출하지 않는다.
- localhost·사설 IP·리디렉션 후 사설망 URL 요청이 차단된다.
- 완료된 작업에 complete를 다시 호출해도 AI가 중복 호출되지 않는다.
- 잘못된 상태 전이 요청이 거절되는 테스트가 통과한다.

## P0-6b. 보존 정책 정리와 정기 데이터 삭제

`HANDOFF.md`의 P0 "7일 원문 보존 정책 불일치"를 이 항목으로 흡수한다. 핵심은 단순
cron 삭제가 아니라 **대표 원문을 30일 보고서 JSON에서 분리**하는 스키마 변경이다.

**구현 결과 (2026-08-11)**

- 보고서 저장 시 `stripRawFromReport`로 대표 리뷰 원문을 제거하고 `report_json`에는
  `review_id` 참조·별점·분류만 남긴다. 원문은 `reviews` 테이블(7일 만료)에만 보관된다.
- 조회 시 `hydrateStoredReport`가 보존 기간이 남았으면 `reviews`에서 원문을 복원하고,
  만료됐으면 원문을 비운 채 안내를 덧붙인다. probe·캐시 응답·`GET /v1/jobs/{id}`가 모두
  이 공통 함수를 거쳐 만료 원문이 노출되지 않는다.
- `scheduled` 핸들러와 Cron Trigger(`0 */6`)로 요청과 무관하게 7일 지난 원문, 30일 지난
  보고서, 만료 토큰, 오래된 rate 이벤트, 방치된 대기·종료 작업, 멈춘 모바일 작업을 정리한다.
- 원문 분리·복원·만료·cron 정리에 대한 회귀 테스트를 추가했다(전체 131개 통과).
- 배포에는 `wrangler deploy`와 원격 D1에 `0005` 마이그레이션 적용이 남아 있다.

**업데이트 위치**

- `worker/src/index.ts`, `worker/src/analyze.ts`
- 신규 D1 마이그레이션과 개인정보 처리방침

**구현 방법**

- 대표 원문을 `reports.report_json`에서 분리해 별도 원문 테이블로 옮기고, 보고서에는
  개수·요약만 유지한다.
- `GET /v1/jobs/{id}`를 포함한 모든 조회 경로가 공통 직렬화 함수를 거쳐 만료 원문을
  반환하지 않게 한다.
- Scheduled Worker(Cron Trigger)로 7일 지난 원문, 30일 지난 보고서, 만료 토큰,
  중단·고아 작업, 오래된 진단 정보를 정리한다.

**난이도**: M

**장점**: 원문을 필요 이상으로 장기 보존하지 않고 개인정보 위험을 낮춘다.

**단점**: 만료된 보고서는 재수집이 필요하고, 스키마 마이그레이션 시 기존 데이터
이전 절차가 필요하다.

**완료 기준**

- 8일로 시간을 이동한 테스트에서 D1 원문이 실제로 삭제된다.
- 30일 이후 보고서와 관련 작업 데이터가 삭제된다.
- 어떤 조회 경로에서도 만료된 대표 원문이 반환되지 않는다.
- 요청이 없어도 Cron Trigger만으로 만료 정리가 실행된다.

## P1-1. 분석 정확도와 신뢰도 2단계 개선

**업데이트 위치**

- `worker/src/analyze.ts`, `worker/src/analyze.test.ts`
- `src/domain/types.ts`, `src/App.tsx`

**구현 방법**

- 수집 완성도, 리뷰 수, 반복 의견, 최신성, 데이터 건전성을 분리해 계산한다.
- 신뢰도 산식 버전을 보고서에 기록한다.
- 제외 신호 0건의 의미와 실제 점수 차감 사유를 설명한다.
- 근접 중복과 평점·본문 불일치 규칙을 보수적으로 개선한다.
- 신뢰도와 상품 만족도를 명확히 분리한다.

**난이도**: M~L

**장점**: 점수를 사용자가 이해하고 검증할 수 있다.

**단점**: 규칙이 복잡해지고 상품군별 오탐 조정이 필요하다.

**완료 기준**

- 신뢰도 각 구성요소를 보고서에서 개별 설명할 수 있다.
- 산식 버전이 보고서에 기록된다.
- 신뢰도와 상품 만족도가 UI에서 구분된다.
- 같은 fixture에서 로컬/Worker 분석의 핵심 집계가 일치한다.

## P1-2. 분석 근거 원문 연결

**업데이트 위치**

- `worker/src/analyze.ts`
- Worker와 프런트 보고서 타입
- `src/App.tsx`, `src/styles.css`

**구현 방법**

- 장단점마다 언급 수, 비율, 대표 원문, 별점, 옵션, 작성일을 연결한다.
- 7일 원문 보존 기간이 지난 보고서는 근거 원문이 만료됐음을 표시한다.

**난이도**: M

**장점**: 분석 결론을 실제 리뷰로 검증할 수 있다.

**단점**: 화면이 길어지고 원문 만료 이후에는 일부 근거가 사라진다.

**완료 기준**

- 각 장단점에 언급 수·비율·대표 원문·별점·옵션·작성일이 연결된다.
- 7일 원문 보존 기간이 지난 보고서는 근거 원문 만료를 명확히 표시한다.

## P1-3. 옵션별 분석

**업데이트 위치**

- `extension/content.js`
- `worker/src/analyze.ts`
- `src/domain/types.ts`, `src/App.tsx`

**구현 방법**

- 옵션명을 정규화하고 여러 옵션이 섞이면 알린다.
- 최소 표본을 충족한 옵션만 개별 분석한다.
- 전체와 특정 옵션을 선택할 수 있게 한다.
- 배송과 포장 평가는 제품·옵션 평가에서 분리한다.

**난이도**: M

**장점**: 용량, 색상, 세트가 다른 리뷰의 혼합 오분석을 줄인다.

**단점**: 옵션명이 제각각이고 옵션별 표본이 작아질 수 있다.

**완료 기준**

- 최소 표본을 충족한 옵션만 개별 분석 결과를 표시한다.
- 여러 옵션이 섞이면 사용자에게 알린다.
- 배송·포장 평가가 제품·옵션 평가에서 분리된다.

## P1-4. 최근 기록, 저장, 다시 검사

**업데이트 위치**

- `src/App.tsx`와 신규 프런트 저장 모듈
- 필요할 경우 Worker 사용자 세션 API와 D1 마이그레이션

**구현 방법**

- 첫 단계는 계정 없이 기기에 최근 상품, 수집일, 신뢰도, 만료일을 저장한다.
- 다시 검사와 기록 삭제를 제공한다.
- 기기 간 동기화가 필요해질 때 익명 토큰 또는 계정을 검토한다.

**난이도**: S~M

**장점**: 이전 결과를 쉽게 찾고 재검사할 수 있다.

**단점**: Safari 데이터를 삭제하면 로컬 기록이 사라진다.

**완료 기준**

- 계정 없이 기기에 최근 상품·수집일·신뢰도·만료일이 저장된다.
- 다시 검사와 기록 삭제가 동작한다.

## P1-5. 네이버 URL 유형 통합

**업데이트 위치**

- `src/domain/url.ts`
- `extension/site-configs.js`, `extension/content.js`
- `collector/src/url-safety.ts`, `collector/src/adapters/naver.ts`

**구현 방법**

- 브랜드, 모바일 브랜드, 스마트스토어, 모바일 스마트스토어, 쇼핑 카탈로그, 광고
  파라미터, `#REVIEW` 주소를 구분한다.
- 가능한 범위에서 canonical URL과 상품 ID로 정규화한다.
- 카탈로그와 판매자 리뷰의 출처 차이를 보고서에 표시한다.
- (HANDOFF 이월) `POST /v1/products/resolve`는 서버 정규화를 실제로 구현하거나
  사용하지 않으면 제거한다. 현재는 받은 product를 그대로 돌려줄 뿐이다.
- (HANDOFF 이월) `/v1/jobs/{id}/refresh`와 프런트 다시 불러오기 흐름을 하나로
  통합한다. 기존 보고서를 보존한 채 새 queued 작업을 만들고 성공 시 교체한다.

**난이도**: M

**장점**: 주소 형식으로 인한 실패와 중복 작업을 줄인다.

**단점**: 카탈로그와 판매자 상품의 리뷰 범위가 다를 수 있다.

**완료 기준**

- 각 네이버 URL 유형이 canonical URL·상품 ID로 정규화된다.
- `products/resolve`가 실제 정규화를 하거나 제거된다.
- 다시 불러오기가 기존 보고서를 보존하며 성공 시 교체하는 단일 흐름으로 동작한다.

## P1-6. 사용자 오류 제보와 분석 피드백

**업데이트 위치**

- `src/App.tsx`
- Worker 피드백 API와 D1 테이블
- 운영자 화면

**구현 방법**

- 리뷰 수 불일치, 분석 이상, 다른 상품 리뷰 혼입, 옵션 오류, 기타 의견을 작업 ID와
  함께 전송한다.
- 자유 입력은 선택 사항으로 두고 개인정보 안내를 제공한다.

**난이도**: S~M

**장점**: 실사용 품질 문제를 빠르게 발견하고 우선순위를 데이터로 정할 수 있다.

**단점**: 운영자가 피드백을 확인할 절차와 개인정보 관리가 필요하다.

**완료 기준**

- 리뷰 수 불일치·분석 이상·다른 상품 혼입·옵션 오류·기타 의견을 작업 ID와 함께
  전송할 수 있다.
- 운영자 화면에서 접수된 피드백을 확인할 수 있다.

## P2-1. 쿠팡 전용 어댑터

**업데이트 위치**

- `extension/site-configs.js`와 신규 쿠팡 어댑터
- `collector/src/adapters/coupang.ts`
- fixture, 실상품 테스트, `docs/access-matrix.md`

**구현 방법**

- 비로그인 공개 리뷰, 전체 목록, 최신순, 별점 필터, 옵션·작성일·본문, 모바일 Safari,
  접근 제한 빈도를 먼저 기술 검증한다.
- 실제 상품 10개 이상에서 검증된 뒤에만 지원 상태를 공개한다.

**난이도**: L~XL

**장점**: 활용 범위가 크게 넓고 네이버에 리뷰가 없는 상품의 대안이 될 수 있다.

**단점**: 접근 제한, 앱 전환, 화면 변경, 유지보수 비용이 클 수 있다.

## P2-2. 동일 상품 대체 출처

**업데이트 위치**

- 신규 상품 매칭 모듈
- Worker API와 D1 상품 매칭 테이블
- `src/App.tsx`와 각 사이트 어댑터

**구현 방법**

- 브랜드, 제조사, 모델 번호, GTIN, 용량, 수량, 색상, 세대, 옵션, 카테고리를 비교한다.
- `exact`만 자동 대체하고 `probable`은 사용자 확인, `uncertain`은 사용하지 않는다.
- 요청 판매처, 실제 리뷰 출처, 대체 이유와 일치 근거를 표시한다.
- 초기에는 자동 대체보다 후보를 보여주고 사용자가 고르는 방식을 사용한다.

**난이도**: L

**장점**: 원래 URL의 리뷰에 접근할 수 없어도 분석 가능성이 높아진다.

**단점**: 다른 세대나 옵션을 같은 상품으로 오인할 위험과 판매자 평가 차이가 있다.

## P2-3. 전용 iPhone 앱

**업데이트 위치**

- 현재 Safari 앱 타깃과 신규 iOS 화면
- iOS Share Extension
- Worker 작업 API와 앱·확장 상태 공유 저장소

**구현 방법**

- Safari 공유 메뉴 URL 전달, 진행 상태, 완료 알림, 보고서 열기, 확장 권한 진단을
  우선 구현한다.
- 이후 최근 보고서, 즐겨찾기, 재검사, 가족 공유, 비교 기능을 추가한다.
- 실제 수집은 기존 Safari Web Extension을 재사용한다.

**난이도**: XL

**장점**: URL 복사와 웹 이동을 줄이고 제품다운 경험을 제공한다.

**단점**: Swift 코드, iOS 백그라운드 제한, App Store 심사와 권한 설명이 추가된다.

## P2-4. 여러 상품 비교

**업데이트 위치**

- Worker 분석 모듈
- 프런트 비교 화면
- 저장한 보고서 기능

**구현 방법**

- 두세 상품의 별점 분포, 수집 수, 신뢰도, 장단점, 옵션, 최근 부정 의견, 반복 불만을
  비교한다.
- 서로 다른 표본과 신뢰도를 같은 조건처럼 보이지 않도록 경고한다.

**난이도**: M~L

**장점**: 실제 구매 결정 가치와 서비스 차별성이 커진다.

**단점**: 리뷰 수와 신뢰도가 다른 상품의 단순 비교는 왜곡될 수 있다.

## 5. 보류 또는 비권장 기능

- CAPTCHA 자동 풀이, 지문 위장, 프록시·계정 순환과 접근통제 우회
- 모든 쇼핑몰을 하나의 규칙으로 처리하는 범용 스크레이퍼
- AI가 운영 코드를 자동 수정하고 검증 없이 자동 배포하는 기능
- 상품명만 이용한 자동 대체
- 네이버 iPhone 흐름보다 Android Firefox 확장을 먼저 개발하는 순서
- 리뷰 원문을 필요 이상으로 장기간 저장하는 방식

## 6. 권장 릴리스 순서

### 1차: 가족이 안정적으로 사용하는 베타

- P0-1 네이버 어댑터 안정화
- P0-2 오류 진단
- P0-3 회귀 테스트와 canary
- P0-4 TestFlight
- P0-5 릴리스 자동화
- P0-6a 보안·인증·요청 제한(상태 전이 검증 포함)
- P0-6b 보존 정책 분리·정기 삭제

### 2차: 결과를 믿을 수 있는 제품

- P1-1 신뢰도 개선
- P1-2 분석 근거 연결
- P1-3 옵션별 분석
- P1-4 최근 기록과 재검사
- P1-5 네이버 URL 유형 통합
- P1-6 사용자 피드백

### 3차: 쇼핑몰과 출처 확장

- P2-1 쿠팡 전용 어댑터
- P2-2 동일 상품 대체 출처

### 4차: 전용 앱과 비교 경험

- P2-3 전용 iPhone 앱
- P2-4 여러 상품 비교

## 7. 배포 전 공통 품질 기준

- 실제 상품 10~15개 검증
- 원본 리뷰 수와 별점 분포 대조
- 별점별 최대 100개 최신 리뷰 수집
- 0개 별점과 리뷰 없는 상품 정상 처리
- 수집 실패와 리뷰 없음 구분
- 옵션이 여러 개인 상품 검증
- CAPTCHA 없음과 사용자 처리 경로 검증
- Safari 종료와 네트워크 중단 후 재개
- 캐시 결과와 새 수집 결과 구분
- 확장과 어댑터 버전 확인
- 실패 시 잘못된 보고서 생성 차단
- `npm run check` 통과
- 실제 iPhone 설치와 TestFlight 업데이트 확인
- 데이터 만료와 정기 삭제 확인

## 8. 진행 원칙

- P0-1부터 P2-4까지 한 번에 하나씩 구현한다.
- 각 항목마다 코드, 자동 테스트, 실상품 검증, 문서를 함께 갱신한다.
- 현재 항목의 완료 기준을 통과하기 전에는 다음 항목으로 넘어가지 않는다.
- 예외: 현재 항목이 외부 요인(Apple 멤버십, 사이트 접근성, 외부 심사)으로 대기
  상태면, 그 항목을 열어둔 채 다음 순위의 코드 진행 가능 항목을 병행한다. 병행도
  한 번에 하나만 진행한다.
- 각 P0/P1 항목은 측정 가능한 완료 기준을 반드시 명시한다.
- 사이트 접근 제한을 우회하지 않는다.
- 잘못된 보고서를 보여주는 것보다 명확히 중단하는 것을 우선한다.
- 기존 사용자 데이터와 운영 환경을 변경할 때는 복구 방법을 먼저 준비한다.
